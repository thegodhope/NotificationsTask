const getWalletBalance = async(userId) => {
    // ASSUMPTION: this function gets a user's wallet balanc
    const walletBalance = 50;
    return walletBalance;
}

module.exports = {
    getWalletBalance
}